

npm install --save-dev bootstrap

在main.js中引入

import 'bootstrap/dist/css/bootstrap.min.css';
import 'bootstrap/dist/js/bootstrap.min.js';

然后运行，结果按照提示让执行这样的命令

npm install --save popper.js

npm i vue-aplayer 音乐盒

页面回退
npm run build 项目打包

vue-video-player 视频播放器

v-select-search




云服务器端
apt install nginx -y 安装nginx
apt-install -y mongodb-server 安装mongodb
apt-install -y git
apt install -y nodejs
apt install -y npm
配置淘宝镜像 npm config set registry https://registry.npm.taobao.org
apt install nrm 切换镜像
npm i -g n 升级node
n latest 获取最近版本的node
npm i -g
npm i -g pm2